version: '3.8'

services:
  app:
    build: .
    ports:
      - "4201:4201"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npx ng serve --host 0.0.0.0 --port 4201 --allowed-hosts
